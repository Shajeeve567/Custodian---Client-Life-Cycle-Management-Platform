name: Deploy — Identity Service

on:
  push:
    branches: [main]
    paths:
      - 'backend/src/services/Identity/**'
      - '.github/workflows/deploy-identity.yml'

jobs:
  deploy:
    name: Build & Deploy Identity Service
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore
        run: dotnet restore backend/src/services/Identity

      - name: Build
        run: dotnet build backend/src/services/Identity --no-restore --configuration Release

      - name: Test
        run: dotnet test backend/src/services/Identity --no-build --configuration Release --verbosity normal

      - name: Publish
        run: dotnet publish backend/src/services/Identity --configuration Release --output ./publish/identity

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_IDENTITY_APP_NAME }}
          publish-profile: ${{ secrets.AZURE_IDENTITY_PUBLISH_PROFILE }}
          package: ./publish/identity
